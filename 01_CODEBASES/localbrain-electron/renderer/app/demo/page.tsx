/**
 * React Query Demo Page - T011 Implementation
 * ===========================================
 *
 * Dedicated page showcasing React Query + SSR integration
 * Accessible at /demo route in development
 */

import { ReactQueryDemo } from '@/components/ReactQueryDemo';

export default function DemoPage() {
  return <ReactQueryDemo />;
}