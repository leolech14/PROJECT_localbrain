name: Specification Validation

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  validate-specifications:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Validate OpenAPI specification
      run: npm run validate:api

    - name: Validate AsyncAPI specification
      run: npm run validate:asyncapi

    - name: Validate JSON Schemas
      run: npm run validate:schemas

    - name: Generate documentation
      run: npm run docs:generate

    - name: Upload documentation artifacts
      uses: actions/upload-artifact@v4
      with:
        name: api-documentation
        path: docs/
        retention-days: 30

  build-and-test:
    runs-on: macos-latest
    needs: validate-specifications

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: latest-stable

    - name: Build LocalBrain with Widget System
      run: |
        cd orchestra-widget-system
        npm run build

    - name: Run Tests
      run: |
        cd orchestra-widget-system
        npm run test

    - name: Validate Widget Integration
      run: |
        cd orchestra-widget-system
        # Add widget-specific validation scripts
        echo "✅ Widget system validation complete"